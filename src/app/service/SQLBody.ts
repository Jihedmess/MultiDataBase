export class SQLBody {
    url:String 
 username :String
 password :String;
 driver :String;
 constructor(url,username,password,driver){
 this.url = url
 this.username = username
 this.password = password
 this.driver = driver
 }
}